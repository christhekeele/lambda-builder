#!/usr/bin/env bash
set -e # Fail script if any command fails

if [ $# -ne 0 ] && [[ "$1" == help ]]; then
cat <<HELP
Determines which files are function files.
HELP
exit 0
fi

find functions \
  -type f \
  -iname "*.py" \
  ! -iname "__init__.py" \
  ! -path "__pycache__" \
  -printf "%p " \
